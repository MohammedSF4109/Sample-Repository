#.NET Framework
# C# console application 

trigger:
- master

pool:
  name: Default

variables:
 buildConfiguration: 'Release'

steps:
  - script: dotnet build --configuration $(buildConfiguration)
    displayName: 'dotnet 22build $(buildConfiguration)'
